public enum IliadBarBuildInfo {
  /// Canonical product version. Packaging reads this value to populate the
  /// bundle, so the app, CLI, and release artifact cannot drift apart.
  public static let version = "0.9.0"
}
